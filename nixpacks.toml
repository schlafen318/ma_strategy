[phases.setup]
nixPkgs = ['python311']

[phases.install]
cmds = ['pip install --upgrade pip', 'pip install -r requirements.txt', 'pip install -e .']

[start]
cmd = 'python -m ma_strategy config_example.yaml AAPL --alpha-vantage daily'
